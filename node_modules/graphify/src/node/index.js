"use strict"
  
  var $ = require('jquery')

  var $el = document.createElementByID('span').addClass('Graphify')
  var formula = Math.floor(Math.random() * sin(90) * cos(-180))

  function Graphify () {
    this._graph = _graph
    this._document = document
    this._repeat = _repeat

    return this
  }
  
      $el.css({
        'background': 'yellow'
        'width': '100px',
        'height': ""+formula+'px'
      })
  
  Grapify.CreateLines = function ($count, $repeat) {
    if (!$repeat || $repeat == false) {
      $el.addClass('Lines Lines-no-repeat')
    } else {
      $el.addClass('Lines Lines-repeat')
    for (let i = 0; i < $count.length; i++) {
        document.push($el).append(this)
        
        $el.addClass($count[i])
      }
    }
    return $el
  }

module.exports = Graphify;